{
  "name": "unified-app-template",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "typecheck": "vue-tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@unifiedai/sdk": "^0.2.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@vitejs/plugin-vue": "^5.2.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vue-tsc": "^2.1.0"
  }
}
